= render "modal", card: @card do
  = button_to '発動', user_support_path(@user_support, game_id: @game, cpu_game: cpu_game?(@game)), method: :delete, data: { 'bs-dismiss': "modal" }, class: 'btn btn-primary'
